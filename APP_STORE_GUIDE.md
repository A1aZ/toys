# App Store 发布指南

## 📋 发布前检查清单

### 1. Xcode 项目配置

#### Bundle Identifier
- 在 Xcode 项目设置中，设置唯一的 Bundle Identifier
- 格式：`com.yourcompany.PDFToLongImage`（替换 yourcompany 为你的公司/开发者名称）
- 在 App Store Connect 中创建应用时使用相同的 Bundle ID

#### 版本号
- **CFBundleShortVersionString** (Version): `1.0`（用户可见版本号）
- **CFBundleVersion** (Build): `1`（每次提交递增，如 1, 2, 3...）

#### 签名和证书
1. 打开 Xcode 项目设置
2. 选择 Target: `PDFToLongImage`
3. 打开 `Signing & Capabilities` 标签
4. 勾选 `Automatically manage signing`
5. 选择你的 Team（开发者账号）
6. 确保 Bundle Identifier 正确

### 2. 应用图标

创建应用图标资源（在 Xcode 中）：

**必需尺寸：**
- `AppIcon.appiconset` 需要包含以下尺寸：
  - 16x16 (@1x, @2x)
  - 32x32 (@1x, @2x)
  - 128x128 (@1x, @2x)
  - 256x256 (@1x, @2x)
  - 512x512 (@1x, @2x)
  - 1024x1024 (@1x, @2x)

**创建步骤：**
1. 在 Xcode 中，选择项目 → 选择 Target → `General` 标签
2. 找到 `App Icons` 部分
3. 拖入 `AppIcon.appiconset` 或使用 Asset Catalog
4. 确保所有必需尺寸都已填充

**图标设计要求：**
- 使用 PNG 格式
- 不要使用透明背景（macOS 应用图标）
- 图标应该清晰、专业
- 符合 Apple 的设计指南

### 3. 应用截图

准备 App Store 截图（必需）：

**macOS 应用截图要求：**
- 至少需要 1 张截图
- 推荐尺寸：1280 x 800 像素（16:10 比例）
- 或 1440 x 900 像素
- 或 2560 x 1600 像素（Retina）
- 格式：PNG 或 JPEG

**建议截图：**
1. 主界面（未选择文件状态）
2. 选择文件后的界面
3. 转换过程中的界面
4. 转换成功提示

### 4. App Store Connect 配置

#### 创建应用
1. 登录 [App Store Connect](https://appstoreconnect.apple.com)
2. 点击 `我的 App` → `+` → `新建 App`
3. 填写信息：
   - **平台**: macOS
   - **名称**: PDF转长图（或你想要的名称）
   - **主要语言**: 简体中文
   - **Bundle ID**: 选择你在开发者账号中创建的 Bundle ID
   - **SKU**: 唯一标识符（如：pdf-to-long-image-001）

#### 应用信息
- **副标题**: 简洁描述应用功能（最多 30 个字符）
- **类别**: 工具类（Utilities）
- **内容版权**: 你的版权信息
- **年龄分级**: 4+（无限制内容）

#### 定价和销售范围
- 选择价格（免费或付费）
- 选择销售地区

#### 应用隐私
- 本应用**不需要**收集任何用户数据
- 在隐私部分选择：
  - "不收集数据" 或
  - 如果系统要求，选择 "不用于跟踪目的的数据"

### 5. 应用描述

准备以下内容：

**应用描述（简体中文）：**
```
PDF转长图是一款简单易用的 macOS 工具应用，可以将 PDF 文件的所有页面转换为单张垂直拼接的长图。

主要功能：
• 快速选择 PDF 文件
• 高质量转换（2x 缩放）
• 自动拼接所有页面
• 保存为 PNG 格式
• 简洁美观的用户界面

使用方法：
1. 点击"选择 PDF 文件"按钮
2. 选择要转换的 PDF 文件
3. 点击"转换为长图"
4. 选择保存位置
5. 完成！

适用于需要将 PDF 文档转换为图片的场景，如文档分享、演示文稿制作等。
```

**关键词（最多 100 个字符）：**
```
PDF,转图片,长图,拼接,文档转换,工具,图片处理
```

**宣传文本（可选，最多 170 个字符）：**
```
快速将 PDF 转换为高质量长图，简单易用，界面美观。
```

### 6. 构建和上传

#### 构建应用
1. 在 Xcode 中，选择 `Product` → `Archive`
2. 等待归档完成
3. 在 Organizer 窗口中，选择刚创建的归档
4. 点击 `分发 App`
5. 选择 `App Store Connect`
6. 选择 `上传`
7. 按照向导完成上传

#### 验证构建
- 上传后，在 App Store Connect 中等待处理（通常几分钟到几小时）
- 在 `TestFlight` 中可以测试构建
- 在 `App Store` 标签中，选择构建版本

### 7. 提交审核

1. 在 App Store Connect 中，完成所有必需信息
2. 选择构建版本
3. 填写审核信息（如果需要）
4. 点击 `提交以供审核`

### 8. 审核注意事项

**确保应用符合以下要求：**

✅ **功能完整性**
- 应用可以正常打开
- 所有功能正常工作
- 错误处理完善

✅ **用户界面**
- 界面美观、专业
- 符合 macOS 设计规范
- 支持深色模式（可选但推荐）

✅ **隐私和安全**
- 不收集用户数据
- 不访问网络（除非必要）
- 文件访问权限明确

✅ **性能**
- 应用响应迅速
- 内存使用合理
- 无崩溃或卡顿

### 9. 常见审核被拒原因

⚠️ **避免以下问题：**

1. **缺少应用图标**
   - 确保所有尺寸的图标都已添加

2. **功能不完整**
   - 确保所有功能都能正常工作
   - 测试各种 PDF 文件

3. **描述不准确**
   - 确保应用描述与实际功能一致

4. **缺少隐私政策**（如果收集数据）
   - 本应用不收集数据，通常不需要

5. **崩溃或错误**
   - 充分测试应用
   - 处理各种边界情况

### 10. 发布后维护

- **版本更新**：每次更新时递增版本号和构建号
- **用户反馈**：及时回复 App Store 评论
- **Bug 修复**：快速修复发现的问题
- **功能改进**：根据用户反馈持续改进

## 📝 快速检查清单

在提交前，确认：

- [ ] Bundle Identifier 已设置且唯一
- [ ] 版本号和构建号已更新
- [ ] 应用图标已添加所有尺寸
- [ ] 应用截图已准备（至少 1 张）
- [ ] App Store Connect 中已创建应用
- [ ] 应用描述和关键词已填写
- [ ] 已构建并上传到 App Store Connect
- [ ] 已选择构建版本并提交审核
- [ ] 应用已充分测试，无崩溃

## 🎉 完成！

提交后，通常 24-48 小时内会收到审核结果。如果被拒，根据反馈修改后重新提交。

祝发布顺利！
